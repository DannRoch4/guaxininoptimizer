"""Numero da versao do Guaxinim Optimizer.

O BUILD e incrementado automaticamente a cada execucao de build.bat (ver bump_version.py) —
assim o rodape do app sempre mostra exatamente qual build o usuario esta rodando, o que
ajuda demais quando alguem reporta um problema."""

MAJOR = 1
MINOR = 0
BUILD = 4

VERSION = f"{MAJOR}.{MINOR}.{BUILD}"
